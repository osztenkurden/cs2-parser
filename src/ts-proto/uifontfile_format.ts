// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               v7.34.1
// source: uifontfile_format.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "";

export interface CUIFontFilePB {
  font_file_name?: string | undefined;
  opentype_font_data?: Uint8Array | undefined;
}

export interface CUIFontFilePackagePB {
  package_version?: number | undefined;
  encrypted_font_files: CUIFontFilePackagePB_CUIEncryptedFontFilePB[];
}

export interface CUIFontFilePackagePB_CUIEncryptedFontFilePB {
  encrypted_contents?: Uint8Array | undefined;
}

function createBaseCUIFontFilePB(): CUIFontFilePB {
  return { font_file_name: undefined, opentype_font_data: undefined };
}

export const CUIFontFilePB: MessageFns<CUIFontFilePB> = {
  encode(message: CUIFontFilePB, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.font_file_name !== undefined) {
      writer.uint32(10).string(message.font_file_name);
    }
    if (message.opentype_font_data !== undefined) {
      writer.uint32(18).bytes(message.opentype_font_data);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CUIFontFilePB {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCUIFontFilePB();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.font_file_name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.opentype_font_data = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CUIFontFilePB {
    return {
      font_file_name: isSet(object.fontFileName)
        ? globalThis.String(object.fontFileName)
        : isSet(object.font_file_name)
        ? globalThis.String(object.font_file_name)
        : undefined,
      opentype_font_data: isSet(object.opentypeFontData)
        ? bytesFromBase64(object.opentypeFontData)
        : isSet(object.opentype_font_data)
        ? bytesFromBase64(object.opentype_font_data)
        : undefined,
    };
  },

  toJSON(message: CUIFontFilePB): unknown {
    const obj: any = {};
    if (message.font_file_name !== undefined) {
      obj.fontFileName = message.font_file_name;
    }
    if (message.opentype_font_data !== undefined) {
      obj.opentypeFontData = base64FromBytes(message.opentype_font_data);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CUIFontFilePB>, I>>(base?: I): CUIFontFilePB {
    return CUIFontFilePB.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CUIFontFilePB>, I>>(object: I): CUIFontFilePB {
    const message = createBaseCUIFontFilePB();
    message.font_file_name = object.font_file_name ?? undefined;
    message.opentype_font_data = object.opentype_font_data ?? undefined;
    return message;
  },
};

function createBaseCUIFontFilePackagePB(): CUIFontFilePackagePB {
  return { package_version: undefined, encrypted_font_files: [] };
}

export const CUIFontFilePackagePB: MessageFns<CUIFontFilePackagePB> = {
  encode(message: CUIFontFilePackagePB, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.package_version !== undefined) {
      writer.uint32(8).uint32(message.package_version);
    }
    for (const v of message.encrypted_font_files) {
      CUIFontFilePackagePB_CUIEncryptedFontFilePB.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CUIFontFilePackagePB {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCUIFontFilePackagePB();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.package_version = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.encrypted_font_files.push(
            CUIFontFilePackagePB_CUIEncryptedFontFilePB.decode(reader, reader.uint32()),
          );
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CUIFontFilePackagePB {
    return {
      package_version: isSet(object.packageVersion)
        ? globalThis.Number(object.packageVersion)
        : isSet(object.package_version)
        ? globalThis.Number(object.package_version)
        : undefined,
      encrypted_font_files: globalThis.Array.isArray(object?.encryptedFontFiles)
        ? object.encryptedFontFiles.map((e: any) => CUIFontFilePackagePB_CUIEncryptedFontFilePB.fromJSON(e))
        : globalThis.Array.isArray(object?.encrypted_font_files)
        ? object.encrypted_font_files.map((e: any) => CUIFontFilePackagePB_CUIEncryptedFontFilePB.fromJSON(e))
        : [],
    };
  },

  toJSON(message: CUIFontFilePackagePB): unknown {
    const obj: any = {};
    if (message.package_version !== undefined) {
      obj.packageVersion = Math.round(message.package_version);
    }
    if (message.encrypted_font_files?.length) {
      obj.encryptedFontFiles = message.encrypted_font_files.map((e) =>
        CUIFontFilePackagePB_CUIEncryptedFontFilePB.toJSON(e)
      );
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CUIFontFilePackagePB>, I>>(base?: I): CUIFontFilePackagePB {
    return CUIFontFilePackagePB.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CUIFontFilePackagePB>, I>>(object: I): CUIFontFilePackagePB {
    const message = createBaseCUIFontFilePackagePB();
    message.package_version = object.package_version ?? undefined;
    message.encrypted_font_files =
      object.encrypted_font_files?.map((e) => CUIFontFilePackagePB_CUIEncryptedFontFilePB.fromPartial(e)) || [];
    return message;
  },
};

function createBaseCUIFontFilePackagePB_CUIEncryptedFontFilePB(): CUIFontFilePackagePB_CUIEncryptedFontFilePB {
  return { encrypted_contents: undefined };
}

export const CUIFontFilePackagePB_CUIEncryptedFontFilePB: MessageFns<CUIFontFilePackagePB_CUIEncryptedFontFilePB> = {
  encode(
    message: CUIFontFilePackagePB_CUIEncryptedFontFilePB,
    writer: BinaryWriter = new BinaryWriter(),
  ): BinaryWriter {
    if (message.encrypted_contents !== undefined) {
      writer.uint32(10).bytes(message.encrypted_contents);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CUIFontFilePackagePB_CUIEncryptedFontFilePB {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCUIFontFilePackagePB_CUIEncryptedFontFilePB();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.encrypted_contents = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CUIFontFilePackagePB_CUIEncryptedFontFilePB {
    return {
      encrypted_contents: isSet(object.encryptedContents)
        ? bytesFromBase64(object.encryptedContents)
        : isSet(object.encrypted_contents)
        ? bytesFromBase64(object.encrypted_contents)
        : undefined,
    };
  },

  toJSON(message: CUIFontFilePackagePB_CUIEncryptedFontFilePB): unknown {
    const obj: any = {};
    if (message.encrypted_contents !== undefined) {
      obj.encryptedContents = base64FromBytes(message.encrypted_contents);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CUIFontFilePackagePB_CUIEncryptedFontFilePB>, I>>(
    base?: I,
  ): CUIFontFilePackagePB_CUIEncryptedFontFilePB {
    return CUIFontFilePackagePB_CUIEncryptedFontFilePB.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CUIFontFilePackagePB_CUIEncryptedFontFilePB>, I>>(
    object: I,
  ): CUIFontFilePackagePB_CUIEncryptedFontFilePB {
    const message = createBaseCUIFontFilePackagePB_CUIEncryptedFontFilePB();
    message.encrypted_contents = object.encrypted_contents ?? undefined;
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from((globalThis as any).Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return (globalThis as any).Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
