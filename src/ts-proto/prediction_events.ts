// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               v7.34.1
// source: prediction_events.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { CMsgQAngle, CMsgVector } from "./networkbasetypes.js";

export const protobufPackage = "";

export const EBasePredictionEvents = {
  BPE_StringCommand: 128,
  BPE_Teleport: 130,
  BPE_Diagnostic: 16384,
  UNRECOGNIZED: -1,
} as const;

export type EBasePredictionEvents = typeof EBasePredictionEvents[keyof typeof EBasePredictionEvents];

export namespace EBasePredictionEvents {
  export type BPE_StringCommand = typeof EBasePredictionEvents.BPE_StringCommand;
  export type BPE_Teleport = typeof EBasePredictionEvents.BPE_Teleport;
  export type BPE_Diagnostic = typeof EBasePredictionEvents.BPE_Diagnostic;
  export type UNRECOGNIZED = typeof EBasePredictionEvents.UNRECOGNIZED;
}

export function eBasePredictionEventsFromJSON(object: any): EBasePredictionEvents {
  switch (object) {
    case 128:
    case "BPE_StringCommand":
      return EBasePredictionEvents.BPE_StringCommand;
    case 130:
    case "BPE_Teleport":
      return EBasePredictionEvents.BPE_Teleport;
    case 16384:
    case "BPE_Diagnostic":
      return EBasePredictionEvents.BPE_Diagnostic;
    case -1:
    case "UNRECOGNIZED":
    default:
      return EBasePredictionEvents.UNRECOGNIZED;
  }
}

export function eBasePredictionEventsToJSON(object: EBasePredictionEvents): string {
  switch (object) {
    case EBasePredictionEvents.BPE_StringCommand:
      return "BPE_StringCommand";
    case EBasePredictionEvents.BPE_Teleport:
      return "BPE_Teleport";
    case EBasePredictionEvents.BPE_Diagnostic:
      return "BPE_Diagnostic";
    case EBasePredictionEvents.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface CPredictionEvent_Teleport {
  origin?: CMsgVector | undefined;
  angles?: CMsgQAngle | undefined;
  drop_to_ground_range?: number | undefined;
}

export interface CPredictionEvent_StringCommand {
  command?: string | undefined;
}

export interface CPredictionEvent_Diagnostic {
  id?: number | undefined;
  requested_sync?: number | undefined;
  requested_player_index?: number | undefined;
  execution_sync: number[];
}

function createBaseCPredictionEvent_Teleport(): CPredictionEvent_Teleport {
  return { origin: undefined, angles: undefined, drop_to_ground_range: undefined };
}

export const CPredictionEvent_Teleport: MessageFns<CPredictionEvent_Teleport> = {
  encode(message: CPredictionEvent_Teleport, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.origin !== undefined) {
      CMsgVector.encode(message.origin, writer.uint32(10).fork()).join();
    }
    if (message.angles !== undefined) {
      CMsgQAngle.encode(message.angles, writer.uint32(18).fork()).join();
    }
    if (message.drop_to_ground_range !== undefined) {
      writer.uint32(29).float(message.drop_to_ground_range);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CPredictionEvent_Teleport {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCPredictionEvent_Teleport();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.origin = CMsgVector.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.angles = CMsgQAngle.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 29) {
            break;
          }

          message.drop_to_ground_range = reader.float();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CPredictionEvent_Teleport {
    return {
      origin: isSet(object.origin) ? CMsgVector.fromJSON(object.origin) : undefined,
      angles: isSet(object.angles) ? CMsgQAngle.fromJSON(object.angles) : undefined,
      drop_to_ground_range: isSet(object.dropToGroundRange)
        ? globalThis.Number(object.dropToGroundRange)
        : isSet(object.drop_to_ground_range)
        ? globalThis.Number(object.drop_to_ground_range)
        : undefined,
    };
  },

  toJSON(message: CPredictionEvent_Teleport): unknown {
    const obj: any = {};
    if (message.origin !== undefined) {
      obj.origin = CMsgVector.toJSON(message.origin);
    }
    if (message.angles !== undefined) {
      obj.angles = CMsgQAngle.toJSON(message.angles);
    }
    if (message.drop_to_ground_range !== undefined) {
      obj.dropToGroundRange = message.drop_to_ground_range;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CPredictionEvent_Teleport>, I>>(base?: I): CPredictionEvent_Teleport {
    return CPredictionEvent_Teleport.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CPredictionEvent_Teleport>, I>>(object: I): CPredictionEvent_Teleport {
    const message = createBaseCPredictionEvent_Teleport();
    message.origin = (object.origin !== undefined && object.origin !== null)
      ? CMsgVector.fromPartial(object.origin)
      : undefined;
    message.angles = (object.angles !== undefined && object.angles !== null)
      ? CMsgQAngle.fromPartial(object.angles)
      : undefined;
    message.drop_to_ground_range = object.drop_to_ground_range ?? undefined;
    return message;
  },
};

function createBaseCPredictionEvent_StringCommand(): CPredictionEvent_StringCommand {
  return { command: undefined };
}

export const CPredictionEvent_StringCommand: MessageFns<CPredictionEvent_StringCommand> = {
  encode(message: CPredictionEvent_StringCommand, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.command !== undefined) {
      writer.uint32(10).string(message.command);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CPredictionEvent_StringCommand {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCPredictionEvent_StringCommand();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.command = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CPredictionEvent_StringCommand {
    return { command: isSet(object.command) ? globalThis.String(object.command) : undefined };
  },

  toJSON(message: CPredictionEvent_StringCommand): unknown {
    const obj: any = {};
    if (message.command !== undefined) {
      obj.command = message.command;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CPredictionEvent_StringCommand>, I>>(base?: I): CPredictionEvent_StringCommand {
    return CPredictionEvent_StringCommand.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CPredictionEvent_StringCommand>, I>>(
    object: I,
  ): CPredictionEvent_StringCommand {
    const message = createBaseCPredictionEvent_StringCommand();
    message.command = object.command ?? undefined;
    return message;
  },
};

function createBaseCPredictionEvent_Diagnostic(): CPredictionEvent_Diagnostic {
  return { id: undefined, requested_sync: undefined, requested_player_index: undefined, execution_sync: [] };
}

export const CPredictionEvent_Diagnostic: MessageFns<CPredictionEvent_Diagnostic> = {
  encode(message: CPredictionEvent_Diagnostic, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== undefined) {
      writer.uint32(8).uint32(message.id);
    }
    if (message.requested_sync !== undefined) {
      writer.uint32(16).uint32(message.requested_sync);
    }
    if (message.requested_player_index !== undefined) {
      writer.uint32(24).uint32(message.requested_player_index);
    }
    for (const v of message.execution_sync) {
      writer.uint32(32).uint32(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CPredictionEvent_Diagnostic {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCPredictionEvent_Diagnostic();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.id = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.requested_sync = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.requested_player_index = reader.uint32();
          continue;
        }
        case 4: {
          if (tag === 32) {
            message.execution_sync.push(reader.uint32());

            continue;
          }

          if (tag === 34) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.execution_sync.push(reader.uint32());
            }

            continue;
          }

          break;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CPredictionEvent_Diagnostic {
    return {
      id: isSet(object.id) ? globalThis.Number(object.id) : undefined,
      requested_sync: isSet(object.requestedSync)
        ? globalThis.Number(object.requestedSync)
        : isSet(object.requested_sync)
        ? globalThis.Number(object.requested_sync)
        : undefined,
      requested_player_index: isSet(object.requestedPlayerIndex)
        ? globalThis.Number(object.requestedPlayerIndex)
        : isSet(object.requested_player_index)
        ? globalThis.Number(object.requested_player_index)
        : undefined,
      execution_sync: globalThis.Array.isArray(object?.executionSync)
        ? object.executionSync.map((e: any) => globalThis.Number(e))
        : globalThis.Array.isArray(object?.execution_sync)
        ? object.execution_sync.map((e: any) => globalThis.Number(e))
        : [],
    };
  },

  toJSON(message: CPredictionEvent_Diagnostic): unknown {
    const obj: any = {};
    if (message.id !== undefined) {
      obj.id = Math.round(message.id);
    }
    if (message.requested_sync !== undefined) {
      obj.requestedSync = Math.round(message.requested_sync);
    }
    if (message.requested_player_index !== undefined) {
      obj.requestedPlayerIndex = Math.round(message.requested_player_index);
    }
    if (message.execution_sync?.length) {
      obj.executionSync = message.execution_sync.map((e) => Math.round(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CPredictionEvent_Diagnostic>, I>>(base?: I): CPredictionEvent_Diagnostic {
    return CPredictionEvent_Diagnostic.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CPredictionEvent_Diagnostic>, I>>(object: I): CPredictionEvent_Diagnostic {
    const message = createBaseCPredictionEvent_Diagnostic();
    message.id = object.id ?? undefined;
    message.requested_sync = object.requested_sync ?? undefined;
    message.requested_player_index = object.requested_player_index ?? undefined;
    message.execution_sync = object.execution_sync?.map((e) => e) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
